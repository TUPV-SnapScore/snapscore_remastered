import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary =
      Color(0xFFBDD5DB); // Gray-blue shade for main actions
  static const Color secondary =
      Color(0xFFE3EDF7); // Light blue-gray for backgrounds

  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF707A81);

  // Background Colors
  static const Color background = Color(0xFFF4FEFF);
  static const Color surfaceBackground = Color(0xFFF5F7FA);

  // State Colors
  static const Color success = Color(0xFF4CAF50); // Green
  static const Color error = Color(0xFFE53935); // Red
  static const Color warning = Color(0xFFFFA726); // Orange
  static const Color info = Color(0xFF2196F3); // Light Blue

  // Button Colors
  static const Color buttonPrimary = primary;
  static const Color buttonSecondary = secondary;
  static const Color buttonDisabled = Color(0xFFE0E0E0);

  // Border Colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);
}
